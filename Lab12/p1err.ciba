// compute and print to standard output the min of 3 numbers from standard input

int 1a, b, c; // wrong identifier
b = +0; // 0 with + sign not allowed
read(a);
read(b);
read(c);
int min = a;
if (b < min) {
	min = b;
};
if (c < min) {
	min = c;
};
print(min);