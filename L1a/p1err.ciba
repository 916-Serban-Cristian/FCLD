// compute and print to standard output the min of 3 numbers from standard input

int a, b, c;
read(a);
read(b);
read(c);
min = a; // missing "int" keyword for declaring an integer variable
if (b < min) {
	min == b; // wrong operator for assignment, should be '='
};
if (c < min) {
	min = c;
};
print(min);